import {ThemeConfig, theme as baseTheme} from "antd";

export const theme: ThemeConfig = {
    algorithm: baseTheme.defaultAlgorithm,
    token: {

    },
    components: {
        Card: {

        }
    }
}